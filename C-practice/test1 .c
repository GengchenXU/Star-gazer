//main.c
#include<stdio.h>
int main()
{
	int balance=100;
	int target=1000;
	float rate=0.1;
	int year=0;
	do {
		float interest=balance*rate;
		balance=balance+interst;
		year++;
		}whlie(balance>=target);
		printf("%d no.of years to achievetarget balance.\n",year);
		return 0;
		}
